<%= form_with(model: employeer, local: true) do |form| %>
  <% if employeer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employeer.errors.count, "error") %> prohibited this employeer from being saved:</h2>

      <ul>
      <% employeer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name %>
  </div>

  <div class="field">
    <%= form.label :company_name_url %>
    <%= form.text_field :company_name_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
